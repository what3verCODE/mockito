pre-commit:
  commands:
    rust:fmt:
      run: cargo fmt --check
    rust:lint:
      run: cargo clippy
    ts:check:
      run: pnpm biome check

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit $1