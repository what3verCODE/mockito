pre-commit:
  commands:
    fmt:
      run: pnpm biome format . --write && cargo fmt
    lint:
      run: pnpm biome check && cargo check


commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit $1
